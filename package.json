{
  "name": "hardhat-project",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat --network kairos run scripts/deploy.ts",
    "upgrade": "npx hardhat --network kairos run scripts/upgrade.ts",
    "test:proxy": "npx hardhat test test/proxy.test.ts",
    "test:upgrade": "npx hardhat test test/upgrade.test.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.19"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0",
    "dotenv": "^16.4.7",
    "ethers": "^6.13.5"
  }
}
